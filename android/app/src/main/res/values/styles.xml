<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- หน้าจอตอนเปิดแอป — ใช้ชุดเดียวกันทั้งธีมสว่างและธีมมืดของเครื่อง
         (ไฟล์ใน values-night ตั้งค่าเหมือนกันทุกอย่าง) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- ธีมของหน้าต่างหลังจากแอปเริ่มทำงาน (ระหว่างที่ Flutter กำลังโหลด)
         ต้องกำหนดสีตรง ๆ ไม่ใช้ ?android:colorBackground ไม่งั้นเครื่องที่เปิดธีมมืด
         จะได้พื้นดำคั่นก่อนเข้าหน้าแรก -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/npd_splash_background</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
