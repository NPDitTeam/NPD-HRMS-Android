# Changelog - NPD HRMS

## v1.2.5+45 (2026-04-06)

### เพิ่มเติม (Features)
- เพิ่มประเภทการเพิ่มเวลาใหม่ 2 ประเภท:
  - **ค่าเบี๊ยเลี้ยงออกนอกสถานที่** - อนุมัติผ่านแอปตามโฟลว์เดิม
  - **ค่ารักษาพยาบาล** - แสดงสถานะ "รออนุมัติ" แต่ไม่ต้องอนุมัติผ่านแอป (อนุมัติผ่านระบบอื่น)
- เพิ่มฟิลด์ **จำนวนเงิน (บาท)** ในฟอร์มเพิ่มเวลา
  - แสดงเฉพาะเมื่อเลือกประเภท "ค่าเบี๊ยเลี้ยงออกนอกสถานที่" หรือ "ค่ารักษาพยาบาล"
  - บังคับกรอก และตรวจสอบว่าเป็นตัวเลขที่มากกว่า 0
- แสดงจำนวนเงินในประวัติการเพิ่มเวลา (หน้าเพิ่มเวลา)
- แสดงจำนวนเงินในหน้าอนุมัติเพิ่มเวลา (เฉพาะค่าเบี๊ยเลี้ยงออกนอกสถานที่):
  - รายการรออนุมัติ
  - Dialog รายละเอียด
  - ประวัติ 7 วันล่าสุด

### ไฟล์ที่แก้ไข (Flutter)
- `lib/add_time_screen.dart` - เพิ่ม dropdown ใหม่, ฟิลด์จำนวนเงิน, ส่ง amount ไป API
- `lib/models/approve_add_time_log.dart` - เพิ่ม field amount ใน Model
- `lib/approve_add_time_screen.dart` - แสดงจำนวนเงินในหน้าอนุมัติ
- `pubspec.yaml` - อัพเวอร์ชันเป็น 1.2.5+45

### ไฟล์ที่แก้ไข (PHP API - อัปโหลดขึ้นเซิร์ฟเวอร์)
- `manual_time_logs_test.php` - รับ/บันทึก/ส่งคืน field amount
- `approve_add_time_screen_test.php` - ส่งคืน field amount, ซ่อนค่ารักษาพยาบาลจากรายการอนุมัติ

### Database (ต้องรัน SQL)
```sql
ALTER TABLE manual_time_logs ADD COLUMN amount DECIMAL(10,2) DEFAULT NULL;
```

---

## v1.2.4+44
- เวอร์ชันก่อนหน้า
