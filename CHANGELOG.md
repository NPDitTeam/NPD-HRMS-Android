# Changelog - NPD HRMS

## v1.5.1+73 (2026-09-24)

### แก้ไข (Bug fix)
- **แอปพังตอนเข้าหน้าลงเวลาของคนที่ถูกพักงาน**
  `type 'bool' is not a subtype of type 'String?'`
  Odoo ส่ง `false` กลับมาแทนค่าว่างสำหรับช่องข้อความ (เช่น "หมายเหตุ" ที่ไม่ได้กรอก)
  แต่โค้ด cast เป็น String ตรง ๆ จึงพังทันที
  แก้โดยแปลงค่าผ่านตัวช่วยที่มองว่า `false` คือค่าว่าง ใช้กับทุกช่องที่มาจาก Odoo

---

## v1.5.0+72 (2026-09-24)

### เพิ่มเติม (Features)
- **กันลงเวลาระหว่างถูกพักงาน**
  - เข้าหน้าลงเวลาแล้วถ้าอยู่ในช่วงถูกพักงาน จะไม่แสดงปุ่มลงเวลาเลย
  - แสดง **วันที่เริ่ม–สิ้นสุดการพักงาน จำนวนวัน และเหตุผล** ให้เห็นชัด
    พนักงานจะได้รู้เองว่าลงเวลาไม่ได้เพราะอะไรและถึงเมื่อไร ไม่ต้องไปถาม HR
  - ข้อมูลอ่านสดจาก Odoo (แท็บ "พักงาน" ในเมนูข้อมูลพนักงาน) แก้ที่เดียวมีผลทันที
  - ถ้าเช็คสถานะไม่สำเร็จ (เน็ตสะดุด) ยังลงเวลาได้ตามปกติ
    ตั้งใจไม่บล็อก เพราะการลงเวลาห้ามล่ม

### ไฟล์ที่แก้ไข (Flutter)
- `lib/odoo_rpc_service.dart` - เพิ่ม `getSuspensionStatus()` อ่าน employee.suspension
- `lib/checkin_screen.dart` - รับ employeeCode, เช็คตอนเข้าหน้าและตอนรีเฟรช,
  เพิ่มหน้าจอแจ้งเตือนพร้อมวันที่และเหตุผล
- `lib/main.dart` - ส่ง employeeCode ให้ CheckinScreen ทั้งสองจุด
- `pubspec.yaml` - อัพเวอร์ชันเป็น 1.5.0+72

### ฝั่ง Odoo 14 (ขึ้น prod แล้ว)
- โมเดล `employee.suspension` + แท็บ "พักงาน" ในข้อมูลพนักงาน
- เงินเดือนหักวันละ 50% ตลอดช่วงพักงาน รวมวันหยุด และกันหักซ้อนกับขาดงาน
- `/api/employee_suspension` เผื่อใช้กับแอปตัวใหม่ที่คุยกับ Odoo 18

### ต้องทำที่เซิร์ฟเวอร์ (PHP)
อัปเดตตารางเวอร์ชันที่ `get_latest_version_test.php` ให้เป็น 1.5.0
พร้อมใส่ release_notes เพื่อให้ popup แจ้งเตือนในแอปแสดงรายการข้างบน

---

## v1.2.5+45 (2026-04-06)

### เพิ่มเติม (Features)
- เพิ่มประเภทการเพิ่มเวลาใหม่ 2 ประเภท:
  - **ค่าเบี๊ยเลี้ยงออกนอกสถานที่** - อนุมัติผ่านแอปตามโฟลว์เดิม
  - **ค่ารักษาพยาบาล** - แสดงสถานะ "รออนุมัติ" แต่ไม่ต้องอนุมัติผ่านแอป (อนุมัติผ่านระบบอื่น)
- เพิ่มฟิลด์ **จำนวนเงิน (บาท)** ในฟอร์มเพิ่มเวลา
  - แสดงเฉพาะเมื่อเลือกประเภท "ค่าเบี๊ยเลี้ยงออกนอกสถานที่" หรือ "ค่ารักษาพยาบาล"
  - บังคับกรอก และตรวจสอบว่าเป็นตัวเลขที่มากกว่า 0
- แสดงจำนวนเงินในประวัติการเพิ่มเวลา (หน้าเพิ่มเวลา)
- แสดงจำนวนเงินในหน้าอนุมัติเพิ่มเวลา (เฉพาะค่าเบี๊ยเลี้ยงออกนอกสถานที่):
  - รายการรออนุมัติ
  - Dialog รายละเอียด
  - ประวัติ 7 วันล่าสุด

### ไฟล์ที่แก้ไข (Flutter)
- `lib/add_time_screen.dart` - เพิ่ม dropdown ใหม่, ฟิลด์จำนวนเงิน, ส่ง amount ไป API
- `lib/models/approve_add_time_log.dart` - เพิ่ม field amount ใน Model
- `lib/approve_add_time_screen.dart` - แสดงจำนวนเงินในหน้าอนุมัติ
- `pubspec.yaml` - อัพเวอร์ชันเป็น 1.2.5+45

### ไฟล์ที่แก้ไข (PHP API - อัปโหลดขึ้นเซิร์ฟเวอร์)
- `manual_time_logs_test.php` - รับ/บันทึก/ส่งคืน field amount
- `approve_add_time_screen_test.php` - ส่งคืน field amount, ซ่อนค่ารักษาพยาบาลจากรายการอนุมัติ

### Database (ต้องรัน SQL)
```sql
ALTER TABLE manual_time_logs ADD COLUMN amount DECIMAL(10,2) DEFAULT NULL;
```

---

## v1.2.4+44
- เวอร์ชันก่อนหน้า
